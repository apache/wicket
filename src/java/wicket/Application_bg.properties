RequiredValidator=Полето '${label}' е задължително.
TypeValidator='${input}' не е валиден ${type}.

NumberValidator.range=${input} трябва да бъде между ${minimum} и ${maximum}.
NumberValidator.minimum='${input}' трябва да бъде повече от ${minimum}.
NumberValidator.maximum='${input}' трябва да бъде по-малко от ${maximum}.

StringValidator.range='${input}' трябва да бъде между ${minimum} и ${maximum} символа.
StringValidator.minimum='${input}' трябва да бъде поне ${minimum} символа.
StringValidator.maximum='${input}' трябва да бъде най-много ${maximum} символа.

DateValidator.range='${input}' трябва да бъде между ${minimum} и ${maximum}.
DateValidator.minimum='${input}' трябва да бъде след ${minimum}.
DateValidator.maximum='${input}' трябва да бъде преди ${maximum}.

PatternValidator='${input}' не съвпада с шаблона '${pattern}'
EmailAddressPatternValidator='${input}' не е валиден email адрес.

EqualInputValidator='${input0}' от ${label0} и '${input1}' от ${label1} трябва да бъдат еднакви.
EqualPasswordInputValidator=${label0} и ${label1} трябва да съвпадат.

null=Изберете едно.
nullValid=